<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TechShopApp.ViewModels"
             x:Class="TechShopApp.Views.RegisterPage">

    <ContentPage.BindingContext>
        <viewmodels:RegisterViewModel />
    </ContentPage.BindingContext>

    <StackLayout Padding="30">
        <Label Text="Rejestracja" FontSize="24" HorizontalOptions="Center" />
        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
        <Entry Placeholder="Hasło" Text="{Binding Password}" IsPassword="True" />
        <Entry Placeholder="Powtórz hasło" Text="{Binding ConfirmPassword}" IsPassword="True" />
        <Button Text="Zarejestruj" BackgroundColor="#08D9D6" TextColor="White" Command="{Binding RegisterCommand}" />
        <Label Text="{Binding ErrorMessage}" TextColor="Red" HorizontalOptions="Center" />
    </StackLayout>
</ContentPage>
